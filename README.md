# Many-to-Many RNN (Character-Level Text Generation)

A Character-Level text generation model using a Many-to-Many RNN architecture implemented in PyTorch.

## How to Run

1. Install requirements: `pip install -r requirements.txt`
2. Run app: `streamlit run app.py`
